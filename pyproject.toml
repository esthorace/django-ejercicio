[project]
name = "eduit-django"
version = "0.1.0"
description = "Ejercicio Django para alumnos de EducaciónIT"
authors = [{ name = "Esteban H. Acevedo" }]
requires-python = ">=3.14"

dependencies = ["django>=6.1"]

[tool.uv]
package = false

[dependency-groups]
dev = ["djlint>=1.44.2"]
